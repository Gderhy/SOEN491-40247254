[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

# If you're using React Router (client-side routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200