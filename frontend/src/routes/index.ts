// Export all route-related functionality from a single entry point
export { default as AppRoutes } from './AppRoutes';
export { ROUTES, ROUTE_CONFIG, getRoute, isProtectedRoute } from './routeConfig';
export type { RouteConfig } from './routeConfig';
